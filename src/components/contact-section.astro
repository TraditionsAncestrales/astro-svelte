---
import ContactContent from './contact-content.svelte';
import UiSection from './ui-section.astro';

// PROPS ===================================================================================================================================
const {...rest} = Astro.props;
---

<UiSection title="Me contacter" class="relative" {...rest}>
  <ContactContent client:visible class="w-full flex gap-8" />
</UiSection>
