---
import type { z } from 'zod';
import type { ztestimonyItemsP } from '~/data/schemas.ui';
import UiCarousel from './ui-carousel.astro';
import UiSection from './ui-section.astro';

// PROPS ===================================================================================================================================
const {items} = Astro.props;
const options = {
  type: 'fade',
  arrows: false,
  pagination: false,
  autoplay: true,
  easing: 'linear',
  speed: 1000,
  fixedWidth: '100%',
  rewind: true,
};

// TYPES ===================================================================================================================================
export type Props = z.infer<typeof ztestimonyItemsP>;
---

<UiSection title="Témoignages" class="min-h-[30rem] bg-[url(/17.jpg)] bg-cover">
  <UiCarousel>
    {
      items.map(({author, content, title}) => (
        <div>
          <article class="prose mx-auto flex-1 flex flex-col items-center justify-center text-center lg:prose-xl">
            <h5 class="text-3xl italic">{title}</h5>
            <p class="italic">{content}</p>
            <p class="font-bold">{author}</p>
          </article>
        </div>
      ))
    }
  </UiCarousel>
</UiSection>
